# Electron production renderer target.
# Used by Vite when mode=production:
# - vite build
# - npm run build (and electron:build via npm run build)
# This version has no hosted production backend, so it uses local backend.
VITE_API_URL=http://localhost:5106/api
