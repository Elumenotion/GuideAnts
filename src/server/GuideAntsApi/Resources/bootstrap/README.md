Bootstrap resources for required guide/assistant seeding.

Supported seed source formats in each folder:

- A ZIP archive in export format (`*.zip`)
- A folder containing `manifest.json` plus the normal export/import file layout

Folders:

- `guides/` for required guides
- `assistants/` for required assistants

All seed content uses the existing guide/assistant import format.
