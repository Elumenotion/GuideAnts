# Docker browser build target.
# Used by Vite when mode=docker:
# - npm run browser:build:docker
# - docker/build/build_webapi_ui.ps1 (calls browser:build:docker)
# Points to dockerized backend.
VITE_API_URL=/api
